services:
  postgres:
    image: postgres:17-alpine
    hostname: postgres
    container_name: postgres
    environment:
      POSTGRES_USER: root
      POSTGRES_DB: postgres
      POSTGRES_PASSWORD: root
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U root -d ordersystem_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - ordersystem_network

  api-auth:
    build:
      context: ./api-01-auth-gateway
      dockerfile: Dockerfile
    container_name: api-auth-gateway
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ordersystem_auth_db
      - API_ORDERS_URL=http://api-orders:8081
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ordersystem_network

  api-orders:
    build:
      context: ./api-02-orders-crud
      dockerfile: Dockerfile
    container_name: api-orders-crud
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ordersystem_orders_db
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ordersystem_network

volumes:
  postgres_data:
    name: postgres_data

networks:
  ordersystem_network:
    name: ordersystem_network
    driver: bridge
